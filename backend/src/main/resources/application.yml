server:
  address: 0.0.0.0
  port: ${PORT:8080}

spring:
  application:
    name: chwiyakhaenne
  servlet:
    multipart:
      max-file-size: 25MB
      max-request-size: 30MB

chwiyakhaenne:
  cors:
    allowed-origins: ${CHWIYAKHAENNE_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173,http://localhost:5174,http://127.0.0.1:5174}
